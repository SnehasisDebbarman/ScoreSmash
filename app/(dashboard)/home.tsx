import { View, Text, ScrollView } from 'react-native'
import React from 'react'
import * as LocalAuthentication from 'expo-local-authentication';

const Home = () => {
    return (
        <ScrollView className='h-[200%] w-full bg-purple-800'>
            <Text>home</Text>
        </ScrollView>
    )
}

export default Home